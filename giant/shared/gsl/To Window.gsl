{
  (+s)
  [
    set (+window, input ("Window name:"));
    if (equal (window, ""), null,
      {
         ()
         [
            create_window (window);
            insert_into_window
              (window,
               window,
               deref (s),
               "matrix",
               "");
         ]
      });
  ]
}
